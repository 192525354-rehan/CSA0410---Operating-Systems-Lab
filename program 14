#include<stdio.h>

int main() {
    char file[3][20];
    int i;

    for(i=0;i<3;i++) {
        printf("Enter File %d: ",i+1);
        scanf("%s",file[i]);
    }

    printf("\nFiles:\n");

    for(i=0;i<3;i++)
        printf("%s\n",file[i]);

    return 0;
}
